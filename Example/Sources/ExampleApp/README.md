# ExampleApp

SwiftUI screens, notes fixtures, and lifecycle implementations for the example app. App-owned accessibility identifiers are ordinary strings. This target depends on PyxisRuntime and does not depend on test code.
