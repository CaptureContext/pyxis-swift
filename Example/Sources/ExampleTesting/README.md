# ExampleTesting

Exports PyxisModel, PyxisXCTest, and its async automation APIs to the Xcode UI-test target through the example package. This target contains no app code or shared fixtures and is never linked into the app.
