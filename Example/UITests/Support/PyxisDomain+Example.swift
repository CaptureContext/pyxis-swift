import ExampleTesting

extension PyxisDomain {
	internal static let notes: Self = .init(id: "notes", title: "Notes", order: 0)
	internal static let settings: Self = .init(id: "settings", title: "Settings", order: 1)
}
